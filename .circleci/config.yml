version: 2.1
workflows:
  build:
    jobs:
      - build
jobs:
  build:
    docker:
      - image: cimg/node:12.11.0
    steps:
      - checkout
      - run: 
          name: Install dependencies
          command: npm install
      - run:
          name: Build Application
          command: npm run build
    
  
